all:
  children:
    pi:
      hosts:
        pi1:
          ansible_host: "{{ pi_host }}"
          ansible_user: "{{ pi_user }}"
          ansible_password: "{{ pi_password }}"
    do:
      hosts:
        do1:
          ansible_host: "{{ do_host }}"
          ansible_user: "{{ do_user }}"
          ansible_password: "{{ do_password }}"
                