all:
  children:
    pi:
      hosts:
        pi1:
          ansible_host: "{{ pi_host }}"
          ansible_user: "{{ pi_user }}"
    do:
      hosts:
        do1:
          ansible_host: "{{ do_host }}"
          ansible_user: "{{ do_user }}"
    hets:
      hosts:
        hets1:
          ansible_host: "{{ hets_host }}"
          ansible_user: "{{ hets_user }}"
                